python-telegram-bot[job-queue]
aiohttp
beautifulsoup4
# lxml ускоряет парсинг, но не обязателен — если не ставится
# (например на Windows без компилятора), бот сам переключится
# на встроенный html.parser. Раскомментируй строку ниже, если
# хочешь ставить lxml:
# lxml
